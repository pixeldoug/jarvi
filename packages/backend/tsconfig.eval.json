{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist-eval",
    "noEmit": true
  },
  "include": ["src/**/*", "evals/**/*"],
  // `ts-node.files` is inherited from tsconfig.json (needed for src/types/*.d.ts).
}
